
{{indexmenu_n>20}}

# Static DHCP

The Static DHCP section lets you configure IP address [DHCP Leases](/glossary/d/dhcp_lease) for connected devices. |

##

^ Item ^ Description ^
| {{/icon/edit.png}} | Add a device to the static [DHCP](/glossary/d/dhcp) list |
| Device Name | [Hostname](/glossary/h/hostname) for [IPv4](/glossary/i/ip) |
| MAC Address | Client [MAC Address](/glossary/m/mac).  |  |
| IP Address | IP address for [IPv4](/glossary/i/ip) |
| DUID | [DUID](/glossary/d/duid) for [IPv6](/glossary/i/ip) |
| Host ID | [Host ID](/glossary/h/hostid) for [IPv6](/glossary/i/ip) |

## Add Static DHCP Lease

To add a static DHCP lease:

* Add an existing client or create a lease from scratch:

    * To select an existing client:

        * Click **Add connected host** to open the list

        * Select the desired client

        * Click the {{/icon/add.png}} **add** button

    * To add a static DHCP lease manually:

        * Only click the {{/icon/add.png}} **add** button


The information for existing client is added automatically, otherwise the fields will be empty.

* Add or edit the client information as neeed.

* Click **Save**
